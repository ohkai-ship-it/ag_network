# SPRINT-2026-01 — Start

## Metadata
- **Sprint ID:** SPRINT-2026-01
- **Start date:** 2026-01-29
- **Branch:** main
- **Version:** v0.2
- **Roles:** Kai (PM/HITL), Jeff (Sr Eng/Arch), Jacob (Jr Eng)

## Sprint goals (ranked)
1. Docs + backlog/bug/sprint templates + indexes (persistent)
2. GitHub flow docs (slow, explained, aligned with repo reality)
3. Quick code review (structured findings → backlog/bugs)
4. Review focus order: CLI → performance → observability

## Scope (committed)
- BI-0001 (Docs + backlog/bug/sprint templates + indexes)

## Out of scope
- Evidence bug fix (postponed until repro exists)

## Invariants (must not regress)
- Workspace isolation hard (no cross-workspace reads/writes)
- No global fallbacks (DB/storage/runs)
- Truthful CLI labeling (deterministic vs agent; retrieved vs generated; cached vs fetched)
- Auditability (sources + evidence chain verifiable)
- Determinism by default (LLM/enrichment opt-in)

## Baselines to capture (before changes)
- ruff / pytest status (local, offline)
- CLI help snapshot (for CLI review)
- A small perf baseline (cold + warm run time)

## PR plan
- PR1: add docs/dev structure + templates + indexes + BI-0001
