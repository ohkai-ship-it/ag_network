# BD Copilot Environment Configuration
# Copy this file to .env and fill in values as needed

# Database path (relative to project root)
BD_DB_PATH=data/bd.sqlite

# Logging level (DEBUG, INFO, WARNING, ERROR)
BD_LOG_LEVEL=INFO

# Runs directory
BD_RUNS_DIR=runs

# Default model for generation (for future LLM integration)
# BD_MODEL=gpt-4

# API keys (if needed in future)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
